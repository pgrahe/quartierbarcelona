import LegalDoc from '../components/LegalDoc'

/** /politica-de-privacidad — the RGPD text, in the language of the URL. */
export default function PrivacyPage() {
  return <LegalDoc doc="privacy" />
}
